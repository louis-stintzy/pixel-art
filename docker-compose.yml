version: '3'

services:
  pixelartmaker:
    image: lstz/pixelartmaker:latest
    ports:
      - '3100:3100'
    networks:
      - pixelartmaker-network

networks:
  pixelartmaker-network:
    external: true
